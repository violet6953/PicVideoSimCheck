# ============================================
# 前置步骤：安装 PyTorch GPU 版本 (CUDA 12.x)
# ============================================
# 请先确保虚拟环境已激活，然后运行以下命令安装 GPU 版本的 PyTorch：
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#
# 说明：cu121 对应 CUDA 12.1，向后兼容已安装的 CUDA 12.0。
#       PyTorch 官方未提供 CUDA 12.0 专用 wheel，cu121 是唯一兼容方案。
#
# 安装完成后，再运行以下命令安装其余依赖：
#   pip install -r requirements.txt
# ============================================

Pillow>=10.0.0
numpy>=1.24.0
opencv-python>=4.8.0
imagehash>=4.3.1
scikit-image>=0.21.0
scikit-learn>=1.3.0
tqdm>=4.65.0
pyside6>=6.6.0
